import { colors } from './colors';

export const gradients = {
  background: [colors.blue200, colors.blue100],
  button: [colors.blue400, colors.blue500],
};

export const commonStyles = {
  shadow: {
    elevation: 5,
    shadowColor: colors.black,
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.25,
    shadowRadius: 3.84,
  },
}; 